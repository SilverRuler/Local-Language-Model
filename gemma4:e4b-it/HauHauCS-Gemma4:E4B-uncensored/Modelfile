# 1. 베이스 모델 파일 경로 지정 (절대 경로 또는 Modelfile 기준 상대 경로)
FROM ./Gemma-4-E4B-Uncensored-HauhauCS-Aggressive-Q8_K_P.gguf

# 2. 모델 파라미터 설정 (README 권장 세팅 반영)
PARAMETER temperature 1.0
PARAMETER top_p 0.95
PARAMETER top_k 64
PARAMETER num_ctx 8192

# 3. Gemma 전용 템플릿 설정 (llama.cpp의 --jinja 플래그 역할을 대체함)
TEMPLATE """<bos>{{ if .System }}<start_of_turn>system
{{ .System }}<end_of_turn>{{ end }}{{ if .Prompt }}<start_of_turn>user
{{ .Prompt }}<end_of_turn>{{ end }}<start_of_turn>model
{{ .Response }}<end_of_turn>"""
